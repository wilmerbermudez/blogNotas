<!DOCTYPE html>
<html>
    <head>
        <title>{% block title %}{% endblock title %} </title>
        
        {% block stylesheets %}
            <style>
                .example-wrapper { margin: 1em auto; max-width: 600px; width: 100%;}
                .example-wrapper code { background: #F5F5F7; padding: 2px 6px;}
            </style>
        {% endblock %}

    </head>
    <body>
        <div class="example-wrapper">
            <div class="row">
                <div class="col">
                    <a href="{{ path('app_home') }}">HOME</a>
                    <a href="{{ path('app_blog') }}">BLOG</a>
                    <a href="{{ path('app_contacto') }}">CONTACTO</a>
                </div>
            </div>
        </div>
        {% block body %} 
        {% endblock %}
        {% block javascripts %}

        {% endblock %}
    </body>
</html>